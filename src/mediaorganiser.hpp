#ifndef MEDIA_ORG_H
#define MEDIA_ORG_H

// Returns 1 if something went wrong
// Returns 0 if all is fine and the media was organised
int organise_media(std::string &i, std::string &o, bool &cpy);

#endif
